# 项目协作规则

## 版本管理

- 每次代码、配置、文档、脚本、部署文件或其他项目内容更新，都必须同步递增 `config/build_info.py` 中的 `APP_VERSION`。
- 版本号使用 `YYYY.MM.DD.N` 格式；同一天的每次更新递增末尾编号，跨日期更新使用当天日期并从 `1` 开始。
- 提交前验证源码展示的版本号与 `config/build_info.py` 中的 `APP_VERSION` 一致，并在提交说明或变更记录中注明版本更新。

## Git 提交与 GitHub 推送

- 每次完成用户要求的任务并通过必要验证后，立即提交当前任务产生的代码变更。
- 提交前检查 `git status`、`git diff --check`，并排除 `.env`、账号数据、Token、密码、日志及其他运行时敏感文件。
- 提交完成后立即把当前分支推送到已配置的 GitHub 远端，默认远端为 `origin`。
- GitHub 推送失败时，在同一任务内最多重试 3 次；每次重试前重新执行推送命令并记录结果。
- 连续 3 次推送失败后停止本轮重试，保留本地提交；下一次任务开始时优先补推未同步提交，再处理新的代码变更。
- 推送成功后验证本地分支跟踪关系、最新提交哈希和远端分支哈希一致，并确认工作区状态。
- 提交信息使用简洁、可检索的英文 Conventional Commit 风格，例如 `fix: ...`、`feat: ...`、`chore: ...`。

## 验证要求

- 代码任务完成后运行与变更相关的测试；条件允许时运行全量测试、编译检查和 `git diff --check`。
- 未通过验证的变更不作为已完成任务提交；验证失败时先修复或明确记录失败原因。

## 平台兼容要求

- 后续新增或修改代码、配置、脚本、部署文件和依赖时，必须同时考虑并验证：macOS Intel（x86_64）、macOS Apple Silicon（arm64）、Docker ARM64、Docker x86_64 和 Windows。
- 涉及系统、CPU 架构、浏览器、Chromedriver、路径、动态库或容器镜像的逻辑，禁止写死单一平台；应使用明确的平台映射、运行时探测或可配置回退，并为关键分支补充测试。
- Docker 调用宿主机 RoxyBrowser 时，必须保留宿主机 Chromedriver 桥接方案：容器传递原始调试地址给宿主机驱动；桥接不可用时，按容器 CPU 架构选择匹配的 Linux Chromedriver。
- 提交前检查上述五个平台的启动链路、依赖安装方式和文档示例是否一致；无法在当前机器实测的平台，至少使用模拟架构测试或静态配置检查覆盖。
