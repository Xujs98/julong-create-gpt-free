# 本地使用说明

本文档记录本项目在本机 macOS、Windows 和 Docker 环境中的常用构建、推送、更新命令。

## 1. 首次准备

要求：Git、Docker Desktop（使用 Docker 时）和 Python 3。先获取代码并创建本地配置：

```bash
git clone --branch main https://github.com/Xujs98/julong-create-gpt-free.git
cd julong-create-gpt-free
cp .env.example .env
```

Windows PowerShell 使用：

```powershell
Copy-Item .env.example .env
```

编辑 `.env`，至少设置 `WEBUI_AUTH_CODE`；该文件包含运行时密钥，不提交到 Git。

## 2. 本地 Docker 构建与启动

默认宿主机端口为 `5566`，容器内部端口为 `5000`：

```bash
make docker-up
docker compose ps
curl -fsS http://127.0.0.1:5566/login >/dev/null && echo 'WebUI OK'
```

更换宿主机端口：

```bash
APP_PORT=8000 make docker-up
```

查看日志、停止和重建：

```bash
make docker-logs
docker compose down
docker compose up -d --build --force-recreate
```

## 3. Docker 调用宿主机 RoxyBrowser

此模式适用于 Docker 容器运行 WebUI、macOS 宿主机运行 RoxyBrowser 的场景。Roxy、Chrome 和 Chromedriver 保持在 macOS 宿主机命名空间内。

1. 在 macOS 宿主机启动桥接：

```bash
chmod +x tools/roxy-chromedriver-bridge.sh
./tools/roxy-chromedriver-bridge.sh
curl -fsS http://127.0.0.1:9515/status
```

桥接脚本会根据宿主机架构自动选择 Roxy 自带驱动：Intel 使用 `mac-x64`，Apple Silicon 使用 `mac-arm64`。

2. 在 `.env` 设置：

```dotenv
REGISTRATION_DRIVER=roxy
ROXY_API_BASE=http://host.docker.internal:50000
ROXY_DEBUGGER_HOST=host.docker.internal
ROXY_DOCKER_WEBDRIVER_URL=http://host.docker.internal:9515
```

若 Roxy API 不在默认端口，把 `ROXY_API_BASE` 改为实际地址。然后重建容器：

```bash
docker compose up -d --build --force-recreate
docker compose logs -f app
```

桥接不可用时，容器会回退到匹配自身架构的 Linux Chromedriver：Docker x86_64 使用 `linux64`，Docker ARM64 使用 `linux-arm64`。优先使用桥接，以保持 Roxy 和驱动处于同一 macOS 环境。

## 4. 构建 Docker 镜像

在 `main` 分支执行：

```bash
make docker-build
```

指定镜像或标签：

```bash
DOCKER_IMAGE=qq1371446705/turb-gpt-free-register TAG=v1.0.0 make docker-build
```

构建指定 Docker 架构：

```bash
DOCKER_PLATFORM=linux/amd64 make docker-build
DOCKER_PLATFORM=linux/arm64 make docker-build
```

## 5. 推送 Docker 镜像

先登录镜像仓库，再构建并推送 `latest`：

```bash
docker login
make docker-push
```

推送版本标签：

```bash
make docker-push TAG=v1.0.0
```

推送指定镜像仓库：

```bash
DOCKER_IMAGE=qq1371446705/turb-gpt-free-register make docker-push TAG=v1.0.0
```

发布多架构镜像（需要已创建并启用 Docker Buildx）：

```bash
docker buildx build \
  --platform linux/amd64,linux/arm64 \
  -t qq1371446705/turb-gpt-free-register:latest \
  --push .
```

## 6. 从 GitHub 更新本地代码

更新前确认本地没有需要保留的未提交代码：

```bash
git status
git switch main
git pull --ff-only origin main
```

更新代码后重建并启动：

```bash
docker compose up -d --build --force-recreate
docker compose ps
```

只使用已推送的镜像，不在本机编译：

```bash
git pull --ff-only origin main
docker compose pull
docker compose up -d --no-build
```

## 7. 常用检查

```bash
docker compose ps
docker compose logs --tail=100 app
curl -fsS http://127.0.0.1:5566/login >/dev/null && echo 'WebUI OK'
git status --short --branch
```

本机 macOS 部署可使用：

```bash
make macos-up
make macos-update
```
